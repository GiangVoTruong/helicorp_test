services:
  - type: web
    name: helicorp-test
    runtime: node
    buildCommand: pnpm install && pnpm build
    startCommand: pnpm start
    envVars:
      - key: VITE_GEMINI_API_KEY
        sync: false
